apiVersion: v1
kind: Service
metadata:
  name: flight-booking-service
spec:
  type: NodePort
  selector:
    app: flight-booking
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30007
